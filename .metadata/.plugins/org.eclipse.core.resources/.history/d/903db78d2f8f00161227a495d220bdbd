package group01;

import java.awt.Point;
import java.util.ArrayList;		// ArrayList
import java.util.TreeMap;		// Binary Search Tree
import java.util.PriorityQueue; // Heap

public class DynamicConvexHull {
	public static void main(String[] args) {
		PointReader pr = new PointReader();
		ConvexHull ch = new ConvexHull();
		Graph chd = new Graph();
		
		ArrayList<Point> points = pr.readPoints();
		
		// Array implementation
		ArrayList<Point> chArray = ch.calculateConvexHullArray(points);
		chd.draw(chArray);
		
		// Binary search tree implementation
		TreeMap chTree = ch.calculateConvexHullTree(points);
		chd.draw(chTree);
		
		// Heap implementation
		PriorityQueue<Point> chHeap = ch.calculateConvexHullHeap(points);
		chd.draw(chHeap);
	}
}
